{
  "name": "screenshot-service",
  "version": "1.0.0",
  "description": "Screenshot service for Render.com",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "npx playwright install --only-shell chromium"
  },
  "dependencies": {
    "playwright": "^1.49.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
