services:
  - type: web
    name: screenshot-service
    runtime: node
    plan: free
    buildCommand: npx playwright install chromium --with-deps
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
